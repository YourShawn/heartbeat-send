spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:heartbeat_send_test}?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&connectionTimeZone=UTC
    username: ${MYSQL_USER:heartbeat}
    password: ${MYSQL_PASSWORD:heartbeat}
  flyway:
    clean-disabled: false

heartbeat:
  jwt:
    secret: test-secret-please-use-only-in-unit-tests-32
    expiration-ms: 3600000
  public-base-url: http://localhost:8080
  openai:
    api-key:
    base-url: https://api.openai.com/v1
    model: gpt-4o-mini
